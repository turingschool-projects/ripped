<div class="container">
  <h2>All Exercises</h2>
  <% @exercises.each do |exercise| %>
  <h4><%= link_to exercise.name, exercise_path(exercise)%></h4>
  <p><%= exercise.description %></p>
  <% end %>
</div>
